#include<iostream>
#include<vector>
using namespace std;

void lastOccurrence(vector<int>& a){
 //Write a program to copy the contents of one array into another in the reverse order.
    for(int i=a.size(); i>=0; i--){
        
    }
}

int main(){
    int n;
    cout<<"enter the size of vector: ";
    cin>>n;

    // int x;
    // cout<<"Enter value of x: ";
    // cin>>x;

    vector<int> v;
    for(int i=0; i<n; i++){
        int x;
        cin>>x;
        v.push_back(x);
    }

}